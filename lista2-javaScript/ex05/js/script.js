function valor(){
    var celsius = parseInt(prompt("Digite a temperatura em cº: "));
    var conversao = ( 9 * celsius + 160 ) / 5

    alert ("A tempuratura" + celsius + " em fahrenheit é " + conversao);
}